1: create a dark blue navigation bar with light color items.

2: change the color of the main container on ur page to dark red.

3: create a div and add a bg img with a given width and height.

4: create a vertical box and add a fixed non scrolling bg to it.

5: verify that the bg shorthand property works with some of the values skipped.